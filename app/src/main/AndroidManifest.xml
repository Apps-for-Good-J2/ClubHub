<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clubhub">

    <application
        android:name=".Controller"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TempSignIn" />
        <activity android:name=".ClubHubStudent" />
        <activity android:name=".CreateSchoolActivity" />
        <activity android:name=".FindAClub" />
        <activity android:name=".TempDisplayActivity" />
        <activity android:name=".CreateClubActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CreateAccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>